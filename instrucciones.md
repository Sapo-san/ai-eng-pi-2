# Instrucciones M2PI

## Contexto y Objetivos

Eres un/a ingeniero/a de IA en una empresa de HR SaaS con mucha documentación. El equipo de soporte al cliente recibe más de 200 preguntas repetitivas al día sobre políticas, funcionalidades y procedimientos que ya están documentados en FAQs internas y guías. Tu equipo necesita un Chatbot inteligente de soporte para FAQs que pueda responder al instante recuperando información relevante de la documentación de la empresa, sin requerir búsquedas manuales ni intervención de agentes.

### 🎯Objetivos

- Implementar un sistema de FAQs basado en RAG que procese un documento de texto plano, lo divida en chunks de forma inteligente (mínimo 20 chunks), genere embeddings y los almacene para una recuperación eficiente. Esto crea una base de conocimiento consultable a partir de documentación no estructurada.
- Construir un pipeline de consulta que acepte preguntas de usuarios, realice búsqueda vectorial usando métodos k-NN/ANN/rango/híbridos, recupere los chunks relevantes y genere respuestas precisas con un LLM. El sistema debe devolver JSON estructurado con user_question, system_answer y chunks_related para asegurar transparencia y auditabilidad.
- Opcional: implementar un agente evaluador que puntúe la calidad de la respuesta (0–10) según la relevancia de los chunks, la precisión y la completitud. Esto aporta aseguramiento automático de calidad para el sistema RAG.

    ¿Por qué es importante? Los sistemas RAG son fundamentales en las aplicaciones modernas de IA. Permiten que los LLM respondan con precisión usando conocimiento privado, actualizado y específico del dominio, sin necesidad de fine-tuning costoso. Dominar embeddings, búsqueda vectorial y generación aumentada por recuperación te prepara para casos reales como automatización de soporte al cliente, bases de conocimiento internas, análisis de documentos legales y asistentes para documentación técnica.

### 📢Consigna

Crea un chatbot de soporte para FAQs usando RAG que responda preguntas basándose en un documento del sistema. El sistema debe procesar un documento de texto plano, dividirlo en chunks (al menos 20 chunks) y generar embeddings. Para cada pregunta del usuario, devuelve una salida en JSON que contenga user_question, system_answer y chunks_related usados para generar la respuesta. Utiliza métodos de búsqueda vectorial (p. ej., k-NN, ANN, rango o híbridos) para encontrar de forma eficiente los chunks relevantes. Bonus: implementa un agente evaluador que reciba user_question, system_answer y chunks_related y devuelva un puntaje de 0 a 10 junto con una justificación del resultado.

## Entregables del proyecto y requisitos de entrega

Enviar mediante un enlace público al repositorio en Git. Asegurarse de que el repositorio sea autocontenido y que se pueda ejecutar sin depender de elementos externos no documentados.

### 📂Estructura de repositorio esperada:

| Entregable | Archivo/Fromato | Contenido minimo |
|---|---|---|
| Documento fuente | data/faq_document.txt | Documento de texto plano con ≥1000 palabras sobre temas de FAQ (políticas, procedimientos, funcionalidades, etc.). Debe ser lo suficientemente sustancial como para generar 20+ chunks significativos. |
| Script del pipeline de datos | src/build_index.py or notebook.ipynb (o secciones con código) | Código que: (1) carga el documento, (2) divide el texto en chunks (20+ chunks), (3) genera embeddings para todos los chunks y (4) guarda embeddings + chunks en algún almacenamiento (archivo, base de datos o en memoria). Debe ser ejecutable. |
| Script del pipeline de consultas | src/query.py or notebook.ipynb (o secciones con código) | Código que: (1) recibe la pregunta del usuario como entrada, (2) convierte la pregunta a embedding, (3) realiza una búsqueda vectorial (k-NN/ANN/por rango/híbrida), (4) recupera los chunks relevantes, (5) genera una respuesta con un LLM y (6) devuelve un JSON con user_question, system_answer, chunks_related. |
| Salidas de ejemplo | outputs/sample_queries.json | Al menos 3 pares de ejemplo de consulta–respuesta mostrando el formato de salida en JSON con todas las claves requeridas. Debe demostrar que el sistema funciona de punta a punta. |
| README | README.md | Setup: versión de Python, comando de instalación (pip install -r requirements.txt), configuración de API key (export OPENAI_API_KEY=...). Uso: cómo ejecutar el pipeline de indexación, cómo ejecutar el pipeline de consultas, comando de ejemplo con una pregunta de muestra. Estructura del proyecto: descripción de archivos/carpetas. Decisiones técnicas: mención breve de la estrategia de chunking y del método de búsqueda usado. |
| Configuración | .env.example | Plantilla que muestre las variables de entorno requeridas: OPENAI_API_KEY=your-key-here, EMBEDDING_MODEL=text-embedding-3-small, etc. |
| Toma en cuenta lo aprendido en este módulo 2: estrategias de chunking, generación de embeddings (OpenAI o Sentence-Transformers), búsqueda por similitud vectorial y arquitectura RAG (recuperación + generación). Documenta tus decisiones técnicas (¿por qué este método de chunking?, ¿por qué este enfoque de búsqueda?). |  |  |

Recuerda producir código limpio y mantenible, con funciones modulares, documentación completa (README con instalación + uso), gestión de dependencias y manejo de errores. Esto garantiza que el sistema esté listo para producción y pueda ser extendido por otros miembros del equipo.

## Rúbrica de evaluación

A continuación te compartimos la rúbrica que se usará para evaluar el proyecto, incluye requerimientos e indicadores que tomaremos en cuenta para valorar tu entrega. Revísalos con detalle, para asegurar que tu producto cumple con cada uno de ellos.

| Requerimientos | Indicadores |
|---|---|
| Ejecuta un análisis y segmentación eficiente del documento (parsing y chunking), aplicando estrategias de fragmentación que preserven el contexto semántico y aseguren que los fragmentos tengan el tamaño óptimo para el procesamiento del modelo. | El documento se procesa completo (se captura el 100% del contenido).Se generan 20 o más chunks distintos.Cada chunk tiene entre 50 y 500 tokens.La estrategia de segmentación queda documentada explícitamente (método: tamaño fijo con solapamiento, por oraciones, semántica o por párrafos).El código incluye los parámetros necesarios: chunk_size y overlap (si es tamaño fijo) o la definición de límites (si es semántica/por oraciones). |
| Implementa la generación automatizada de embeddings, transformando los fragmentos de texto en representaciones vectoriales de alta calidad mediante modelos especializados, permitiendo una recuperación de información precisa y veloz. | Se generan embeddings para todos los chunks (la cantidad de embeddings coincide con la cantidad de chunks).Se utiliza la API de OpenAI o un modelo de Sentence-Transformers.Los embeddings quedan guardados: en una variable o archivo (JSON, pickle, CSV) o en una base/vector store. |
| Garantiza un formato de salida JSON estructurado, validando que los datos procesados y recuperados se entreguen con una arquitectura de objetos clara, facilitando su integración con la base de datos vectorial o la interfaz de usuario. | El JSON pasa la validación para todas las consultas de prueba. Contiene exactamente estas tres claves: "user_question", "system_answer" y "chunks_related". |
| Implementa un sistema de búsqueda vectorial robusto, integrando una base de datos de vectores (Vector Store) que permita realizar consultas de similitud de alta velocidad utilizando métricas de distancia (como similitud coseno) sobre los embeddings generados. | Implementa al menos una de estas técnicas: k-Nearest Neighbors (k-NN), Approximate Nearest Neighbors (ANN), Range Query o búsqueda híbrida. El código calcula la similitud de forma explícita (por coseno, producto punto o distancia euclidiana), ya sea mostrando la fórmula o llamando a una función de librería.La documentación indica qué método se usa y por qué. |
| Optimiza la calidad y relevancia de la recuperación, ajustando parámetros de búsqueda como el "top-k" y aplicando filtros para asegurar que la información recuperada sea precisa, contextualizada y minimice el ruido en la respuesta final. | Al probar con 3 o más preguntas de ejemplo: - ≥ 80% de los chunks recuperados contienen palabras clave de la pregunta del usuario o la responden de forma directa. - En preguntas de sí/no: recupera chunks con hechos relevantes. - En preguntas de “cómo hacer”: recupera chunks con pasos o procedimiento. - Devuelve 2 a 5 chunks por consulta. |
| Diseña y ejecuta un Data Pipeline de Indexación, estableciendo un flujo automatizado para la ingesta, limpieza y almacenamiento organizado de la base de conocimiento dentro de un motor vectorial, asegurando la integridad de los datos de origen. | Todas las etapas están presentes y funcionan correctamente:- Etapa 1: Carga del documento (lectura del archivo, contemplando el manejo de codificación).- Etapa 2: Segmentación del texto (chunking; genera 20 o más chunks).- Etapa 3: Generación de embeddings (crea vectores para todos los chunks).- Etapa 4: Almacenamiento (se guardan embeddings + texto de chunks en una base de datos, archivo o estructura en memoria).El pipeline es modular (funciones separadas por etapa o bloques de código claramente diferenciados).Se puede verificar el flujo completo: documento de entrada → vectores almacenados con el texto de cada chunk. |
| Construye un Query Pipeline robusto de Recuperación y Generación, orquestando la lógica que conecta la consulta del usuario con el contexto recuperado y el LLM, para producir respuestas fundamentadas (grounded) y precisas. | Todas las etapas están presentes y conectadas entre sí:- Etapa 1: Embedding de la consulta (la pregunta del usuario se convierte en un vector con la misma dimensionalidad que los embeddings de los chunks).- Etapa 2: Búsqueda vectorial (se recuperan los chunks más similares usando un método de búsqueda).- Etapa 3: Ensamblado de contexto (los chunks recuperados se formatean e incorporan al prompt).- Etapa 4: Generación con LLM (la API de OpenAI o un modelo local genera la respuesta usando ese contexto). |
| Demuestra una comprensión profunda de la arquitectura RAG mediante su implementación, aplicando los componentes de forma lógica para resolver problemas de límites de tokens y proporcionar información actualizada sin necesidad de reentrenamiento. | El sistema implementa recuperación y luego generación (flujo en dos pasos visible en el código).El README o los comentarios explican el enfoque: “Esto usa RAG porque…”, mencionando que primero se recuperan chunks relevantes antes de generar la respuesta. La documentación menciona al menos un beneficio de RAG: conocimiento actualizable, transparencia del proceso y/o atribución de fuentes. |
| Asegura una organización modular del código, estructurando el proyecto de forma que la lógica de procesamiento, la generación de embeddings y la orquestación RAG estén claramente separadas y sean fáciles de navegar. | Define ≥4 funciones con nombres descriptivos (p. ej., load_and_chunk_document(), generate_embeddings(), search_similar_chunks(), generate_answer()).Cada función cumple un único propósito claro (≤30 líneas por función).Estructura del archivo: imports arriba, luego funciones y, al final, la ejecución principal (main). |
| Desarrolla documentación técnica exhaustiva, detallando el funcionamiento de los pipelines, las fuentes de datos utilizadas y las instrucciones necesarias para ejecutar el sistema de recuperación semántica. | El README incluye: Descripción del proyecto (≥50 palabras) explicando el propósito del chatbot de preguntas frecuentes (FAQ).Instrucciones de instalación/configuración con 3+ pasos (instalar dependencias, configurar la API key: export OPENAI_API_KEY=your-key y ejecutar: python main.py).Ejemplo de uso que muestre una pregunta de entrada y el formato esperado de salida en JSON.Justificación de decisiones técnicas: por qué se eligió esta estrategia de chunking (1–2 oraciones) y por qué este método de búsqueda vectorial (1–2 oraciones). |
| Gestiona correctamente las dependencias y el entorno, proporcionando los archivos de configuración (como requirements.txt o environment.yml) y las variables de entorno necesarias para garantizar la reproducibilidad del sistema en diferentes entornos. | Incluye requirements.txt (o equivalente) con las dependencias listadas. Especifica versiones (p. ej., openai==1.12.0, numpy==1.24.0).La API key se carga desde una variable de entorno con os.getenv() o desde un archivo de configuración.El README explica cómo definir las variables de entorno. |
| Implementa un Agente Evaluador especializado, diseñando un flujo de supervisión que analice la calidad de las respuestas generadas por el sistema RAG, verificando la fidelidad con respecto al contexto recuperado y detectando posibles alucinaciones o inconsistencias antes de la entrega final. | Devuelve un diccionario/objeto con:"score": entero de 0 a 10"reason": string de ≥50 caracteresLa evaluación considera ≥2 dimensiones, por ejemplo: - Relevancia de los chunks: ¿los chunks se relacionan con la pregunta? - Calidad de la respuesta: ¿la respuesta usa información de los chunks? - Completitud: ¿la respuesta cubre totalmente la pregunta?El "reason" justifica el puntaje con observaciones específicas (p. ej., “Puntaje 8: responde usando 3 chunks relevantes, pero podría incorporar más detalle del chunk 2”). |
